---
interface Props {
  text: string;
  class?: string;
}

const { text, class: className = "" } = Astro.props;
---

<span
  class:list={[
    "inline-flex items-center rounded-full border border-white/10 px-3 py-1 text-xs text-text-muted",
    className,
  ]}
>
  {text}
</span>
